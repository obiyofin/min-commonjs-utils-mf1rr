const { add2 } = require("./index");
const { printMessage } = require("./utils");

//this is result of this
let result = add2(2, 4, "bangkok is cool city");

//console.log(2 times)
printMessage(result);
